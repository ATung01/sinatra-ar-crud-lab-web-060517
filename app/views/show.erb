<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Your Post</title>
  </head>
  <body>
    <p><%= @post.name %></p>
    <p><%= @post.content %></p>
    <form action= <%= "/posts/#{@post.id}/delete" %> method="post">
      <input id="hidden" type="hidden" name="_method" value="delete">
      <input type="submit" name="submit" value="Delete Post" id="delete">
    </form>
  </body>
</html>
